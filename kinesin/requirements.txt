# ARC (AutoResearchClaw) — PyPI package
researchclaw @ git+https://github.com/aiming-lab/AutoResearchClaw.git@main

# ScienceClaw — not pip-installable (custom setup.py); installed by setup_kinesin.sh
# Cloned to ~/scienceclaw; parent dir added to site-packages via .pth file.

# OmicsClaw — no releases; pin to current main commit (March 27, 2026)
# Installs core only; spatial/singlecell extras installed separately (see setup_kinesin.sh)
omicsclaw @ git+https://github.com/TianGzlab/OmicsClaw.git@370058f4400b384b74410a72342cae9b71cbc0dd

# Shared dependencies (version-pin to match existing base-env.yml)
pyyaml>=6.0
pydantic>=2.0.0
anthropic>=0.20.0
requests>=2.28.0

# Core bio_skills dependencies (geo, deseq2, reactome, open_targets, disease_gene_atlas)
GEOparse>=2.0.3
gseapy>=1.1.3

# OmicsClaw spatial skills — lightweight tier (no torch/GPU required)
# Heavy DL deps (SpaGCN, STAGATE, torch) are optional; spatial-domains falls back to Leiden
# without them. Install heavy tier separately: pip install omicsclaw[spatial-domains]
